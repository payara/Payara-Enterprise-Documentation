////

A partial page to include into a feature which is a minimum viable product.
Variable "mvpVersion" specifies the Payara version for the MVP

Include by (for example):

:mvpVersion: "5.25.0"
include::partial$mvp.adoc[]

If {mvpVersion} == {currentVersion}, the fragment will be visible for all versions.

////

NOTE: This feature is currently released as a *Minimum Viable Product* (MVP) and will gradually recieve additional enhancements in future Payara Enterprise releases.

